# generated by datamodel-codegen:
#   filename:  runner.transcript-snapshot.json
#   timestamp: 2026-09-13T23:59:19+00:00

from __future__ import annotations

from pydantic import BaseModel, ConfigDict, Field


class RunnerTranscriptSnapshot(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    transcriptTail: list[str] = Field(
        ...,
        description="The last N raw lines of the run's transcript file (N defaults to the server's own window; `?tail=` overrides it). Always present, even for a still-`launched` run (may then be a shorter or empty tail).",
    )
    result: str | None = Field(
        ...,
        description="The CLI's own final terminal answer, parsed from its terminal `result` line. `null` when the run has not yet produced one (still `launched`, or ended without a parseable result line) — never a placeholder string standing in for absence.",
    )
    resultTruncated: bool = Field(
        ...,
        description="True when `result` was clipped to a server-side budget rather than being the CLI's full, untruncated answer.",
    )
