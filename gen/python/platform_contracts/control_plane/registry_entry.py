# generated by datamodel-codegen:
#   filename:  registry.entry.json
#   timestamp: 2026-07-03T00:55:33+00:00

from __future__ import annotations

from enum import StrEnum

from pydantic import AnyUrl, AwareDatetime, BaseModel, ConfigDict, Field, constr


class Kind(StrEnum):
    runtime = 'runtime'
    app = 'app'
    buildtime = 'buildtime'


class Side(StrEnum):
    host = 'host'
    hub = 'hub'
    ui = 'ui'
    shared = 'shared'


class Status(StrEnum):
    planned = 'planned'
    building = 'building'
    active = 'active'
    suspended = 'suspended'
    deprecated = 'deprecated'


class Class(StrEnum):
    low_stakes = 'low-stakes'
    health_class = 'health-class'
    kids_class = 'kids-class'


class Plan(StrEnum):
    free = 'free'
    pro = 'pro'
    enterprise = 'enterprise'


class RegistryEntry(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    functionalName: constr(pattern=r'^[a-z][a-z0-9-]+$') = Field(
        ..., examples=['treasury', 'control-plane']
    )
    kind: Kind
    side: Side
    status: Status = Field(
        ...,
        description='`suspended` exists only here — a registry action, not a repo property.',
    )
    repoUrl: AnyUrl = Field(..., description='Source repository URL.')
    version: str = Field(
        ...,
        description='Git tag where one exists, else version+shortsha.',
        examples=['v0.2.2', '0.1.0+14d02b2'],
    )
    contractsPin: str | None = Field(
        None,
        description="Mirror of the descriptor's contracts.pin.",
        examples=['v0.2.2'],
    )
    updatedAt: AwareDatetime
    appId: str | None = Field(
        None, description='App-only. Populated when app.manifest registration arrives.'
    )
    class_: Class | None = Field(
        None,
        alias='class',
        description='App-only. D010 risk class tier, populated when app.manifest registration arrives.',
    )
    plan: Plan | None = Field(
        None,
        description="App-only. Populated when app.manifest registration arrives; enum aligned with app.manifest's plan field.",
    )
