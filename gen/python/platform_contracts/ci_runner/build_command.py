# generated by datamodel-codegen:
#   filename:  build-command.yaml
#   timestamp: 2026-07-02T15:41:08+00:00

from __future__ import annotations

from pydantic import BaseModel, ConfigDict, Field, constr


class BuildCommand(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    correlationId: str = Field(
        ...,
        description='Opaque token generated by the caller (control-plane). Echoed in the matching BuildResult so the caller can correlate the async response.\n',
    )
    repo: constr(pattern=r'^[\w.-]+/[\w.-]+$') = Field(
        ...,
        description='Full GitHub repository name — owner/repo, e.g. elmoul/plant-pal',
    )
    ref: str = Field(..., description='Git ref to build: branch name, tag, or full SHA')
    workflow: str | None = Field(
        None,
        description="Workflow file name (e.g. ci.yml) or numeric workflow ID. Defaults to the repo's primary workflow when omitted.\n",
    )
