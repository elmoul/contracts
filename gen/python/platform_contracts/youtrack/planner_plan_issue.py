# generated by datamodel-codegen:
#   filename:  planner.plan-issue.json
#   timestamp: 2026-09-26T03:33:38+00:00

from __future__ import annotations

from pydantic import BaseModel, ConfigDict, Field, constr


class PlannerPlanIssue(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    key: constr(pattern=r'^[a-z0-9][a-z0-9-]{0,39}$') = Field(
        ...,
        description="A key unique within this plan, referenced by `parent`/`dependsOn`/`relatesTo` on sibling issues. Not the tracker's own issue id -- see `planner.plan-run.created` for that.",
    )
    summary: constr(min_length=1, max_length=200) = Field(
        ..., description='The issue title.'
    )
    description: constr(max_length=20000) | None = Field(
        '', description='The issue body.'
    )
    fields: dict[str, str] | None = Field(
        {}, description='Tracker field name to value.'
    )
    parent: str | None = Field(
        None,
        description="The `key` of this issue's parent within the same plan, or `null`.",
    )
    dependsOn: list[str] | None = Field(
        [], description='Keys of issues in the same plan this one depends on.'
    )
    relatesTo: list[str] | None = Field(
        [], description='Keys of issues in the same plan this one relates to.'
    )
