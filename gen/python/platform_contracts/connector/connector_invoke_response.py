# generated by datamodel-codegen:
#   filename:  connector.invoke.response.json
#   timestamp: 2026-07-04T21:00:54+00:00

from __future__ import annotations

from enum import Enum
from typing import Any
from uuid import UUID

from pydantic import BaseModel, ConfigDict, Field


class Status(Enum):
    ok = 'ok'
    refused = 'refused'
    error = 'error'


class ConnectorInvokeResponse(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    requestId: UUID = Field(
        ...,
        description='Echoes the requestId of the connector.invoke.request this responds to.',
    )
    status: Status = Field(
        ...,
        description="Outcome of the invocation. 'refused' is a first-class outcome, not an error — the connector declines a call outside its declared vocabulary, missing/invalid confirmation token, or otherwise against policy. 'error' is an unexpected failure (provider unavailable, etc.).",
    )
    result: dict[str, Any] | None = Field(
        None,
        description="Verb-specific result payload. Present only when status is 'ok'; shape follows the invoked verb's declared returns.",
    )
    reason: str | None = Field(
        None,
        description="Human-readable explanation for a non-ok outcome. Required when status is 'refused' or 'error'; absent when status is 'ok'.",
    )
