# generated by datamodel-codegen:
#   filename:  connector.invoke.request.json
#   timestamp: 2026-07-04T21:00:51+00:00

from __future__ import annotations

from typing import Any
from uuid import UUID

from pydantic import BaseModel, ConfigDict, Field, constr


class ConnectorInvokeRequest(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    requestId: UUID = Field(
        ...,
        description="Unique identifier for this invocation, correlated with the response and the connector's local audit log.",
    )
    caller: constr(pattern=r'^[a-z][a-z0-9-]+$') = Field(
        ...,
        description='Functional name of the requesting hexagon (D002) — e.g. the orchestrator or sentinel-hub.',
        examples=['orchestrator', 'sentinel-hub'],
    )
    verb: str = Field(
        ...,
        description='Verb being invoked. Must match a verb name the target connector declares in its connector.vocabulary; anything else is refused, not an error.',
    )
    params: dict[str, Any] = Field(
        ...,
        description="Verb-specific parameters, shaped per the declared verb's params schema. Deliberately permissive at this level — the connector validates against its own vocabulary.",
    )
    confirmationToken: str | None = Field(
        None,
        description='Short-lived signed token binding {verb, target, content-hash}, issued by the confirmation-card flow. Write verbs require a valid token to succeed; omission or invalidity is a connector-side refusal, not enforced by this schema (spec-connectors.md §6-3).',
    )
