/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

export interface paths {
    "/ai/jobs": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Submit a generation job through the gateway
         * @description Forwarded to media-generation's queue after a $0 Treasury pre-flight. Returns as soon as the job is accepted onto the queue — it does not wait for the job to run.
         */
        post: operations["submitAiJob"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/ai/jobs/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Get the current status of a submitted job */
        get: operations["getAiJobStatus"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
}
export type webhooks = Record<string, never>;
export interface components {
    schemas: {
        /** @description Submitted by a caller (via ai-gateway) to request an async generation job from media-generation (D047). jobId is caller-generated so the caller can start polling/correlating before the 202 response returns. */
        AiJobRequest: {
            /**
             * Format: uuid
             * @description Unique identifier for this job, generated by the caller. Used to poll GET /ai/jobs/{id} and to correlate job.progress state-events.
             */
            jobId: string;
            /**
             * @description Functional app/caller identifier (D002) -- same shape as app.manifest's name / hexagon.descriptor's functionalName / ai.model-manifest's appId. Hub-side callers (design-studio, agent-runner) identify the same way, by functional name (D032) -- no separate class taxonomy for non-app callers.
             * @example plantpal
             * @example design-studio
             */
            appId: string;
            /**
             * @description The generation capability this job requests. Matches one of media-generation's registered CapabilityPort adapters. Video is a reserved capability slot (spec-media-generation.md §8) with no model/spec commitment yet, so it is deliberately not in this enum.
             * @enum {string}
             */
            capability: "text-to-image" | "image-to-3d";
            /** @description Capability-dependent job input. `prompt` for text-to-image; `imageRef` for image-to-3d. Both declared optional at the schema level since which one is meaningful depends on `capability` -- media-generation validates the combination for the requested capability, this schema does not cross-check it. */
            input: {
                /** @description Text prompt for a text-to-image job. */
                prompt?: string;
                /** @description Reference (uploaded asset id or URL) to the source image for an image-to-3d job. */
                imageRef?: string;
            };
            /** @description Capability-specific parameters, passed through by the gateway (e.g. resolution/max_num_view for image-to-3d; steps/aspect ratio for text-to-image). Deliberately permissive at this level, same idiom as connector.invoke.request's params -- media-generation's capability adapter validates against its own declared param schema. */
            params: {
                [key: string]: unknown;
            };
            /**
             * @description interactive runs as soon as VRAM admits; batch holds for the configured night window (conventions rule). Owner can force-run any queued job regardless of class.
             * @enum {string}
             */
            jobClass: "interactive" | "batch";
        };
        /** @description Current state of a submitted job. Returned both by the submit call's 202 response (initial state, normally "queued") and by GET /ai/jobs/{id} (subsequent polls). */
        AiJobStatus: {
            /**
             * Format: uuid
             * @description Echoes the jobId from the originating AiJobRequest.
             */
            jobId: string;
            /**
             * @description Current lifecycle status of the job (media-generation's queue/admission/model-lifecycle states).
             * @enum {string}
             */
            status: "queued" | "admitted" | "loading" | "running" | "succeeded" | "failed" | "cancelled";
            /** @description Best-effort completion percentage while running. Absent when not meaningful (e.g. still queued). */
            progressPct?: number;
            /** @description References to the job's output artifacts (GET /jobs/{id}/artifacts/* paths on media-generation) once succeeded. Absent or empty until results exist. */
            resultRefs?: string[];
            error?: components["schemas"]["JobError"];
            usage?: components["schemas"]["JobUsage"];
            /**
             * Format: date-time
             * @description When the job was accepted onto the queue.
             */
            submittedAt: string;
            /**
             * Format: date-time
             * @description When the job left the queue and began running (admitted/loading/running).
             */
            startedAt?: string;
            /**
             * Format: date-time
             * @description When the job reached a terminal status (succeeded/failed/cancelled).
             */
            finishedAt?: string;
        };
        /** @description Present when status is "failed"; absent otherwise. */
        JobError: {
            /** @description Machine-readable failure code (e.g. "vram-exhausted", "worker-unavailable"). */
            code: string;
            /** @description Human-readable failure detail. */
            message: string;
        };
        /** @description Best-effort resource usage, populated as it becomes known (may be partially populated on a job that failed mid-run). */
        JobUsage: {
            /** @description Wall-clock run duration in milliseconds. */
            durationMs?: number;
            /** @description Peak VRAM usage observed during the job, in megabytes. */
            vramPeakMb?: number;
        };
    };
    responses: never;
    parameters: never;
    requestBodies: never;
    headers: never;
    pathItems: never;
}
export type $defs = Record<string, never>;
export interface operations {
    submitAiJob: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AiJobRequest"];
            };
        };
        responses: {
            /** @description Job accepted onto the queue */
            202: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AiJobStatus"];
                };
            };
            /** @description Cost/resource ceiling reached — request blocked by Treasury (D023) */
            402: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description media-generation unavailable */
            503: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    getAiJobStatus: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Current job status */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AiJobStatus"];
                };
            };
            /** @description No job with this id */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
}
