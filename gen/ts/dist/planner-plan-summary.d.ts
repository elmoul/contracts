/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */
/**
 * A `backlog-planner` plan-run summary, as `GET /plans` serves it (list, newest first) and as `summary_view()` builds it in `youtrack/planner/src/backlog_planner/api.py`. `planner.plan-run` is this shape plus the run's full detail.
 */
export interface PlannerPlanSummary {
    id: string;
    /**
     * Always carries a UTC offset.
     */
    createdAt: string;
    status: "previewed" | "partial" | "confirmed";
    project: {
        /**
         * `null` for a new project until it is actually created in the tracker.
         */
        id: string | null;
        shortName: string;
        name: string;
        isNew: boolean;
    };
    prompt: string;
    model: {
        requested: string;
        used: string | null;
        provider: string | null;
    };
    proposedCount: number;
    createdCount: number;
}
