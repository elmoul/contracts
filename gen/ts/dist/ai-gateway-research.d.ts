/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */
export interface paths {
    "/ai/research-jobs": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Submit a research evidence job through the gateway
         * @description Accepts a list of source URLs to retrieve, each with its own retrieval bounds. Returns as soon as the job is accepted onto the queue -- it does not wait for any source to be fetched.
         */
        post: operations["submitResearchJob"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/ai/research-jobs/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get the current status of a submitted research evidence job
         * @description Reconciliation route: an interrupted caller re-polls this by the same jobId it generated at submission time rather than guessing whether the (paid) retrieval work already ran.
         */
        get: operations["getResearchJobStatus"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
}
export type webhooks = Record<string, never>;
export interface components {
    schemas: {
        /** @description Submitted by a caller (via ai-gateway) to request an async evidence-gathering job. jobId is caller-generated -- same idiom as ai.job.request's jobId -- so the caller can start polling/correlating before the 202 response returns, and can safely retry submission against the same jobId if the initial call is interrupted. */
        ResearchJobRequest: {
            /**
             * Format: uuid
             * @description Unique identifier for this job, generated by the caller. Used to poll GET /ai/research-jobs/{id}.
             */
            jobId: string;
            /**
             * @description Functional app/caller identifier (D002) -- same shape as ai.job.request's appId.
             * @example plantpal
             * @example design-studio
             */
            appId: string;
            /** @description The URLs to retrieve, each with its own retrieval bounds. The gateway fetches each independently -- one source failing does not fail the others. */
            sources: components["schemas"]["SourceRequest"][];
        };
        /** @description A single URL to retrieve, with the bounds the gateway must enforce on it. */
        SourceRequest: {
            /**
             * Format: uri
             * @description The source URL to retrieve.
             */
            url: string;
            /** @description Maximum wall-clock time, in milliseconds, the gateway may spend retrieving this source before treating it as unavailable. */
            maxDurationMs: number;
            /** @description Maximum number of bytes the gateway may read from this source before truncating/abandoning the fetch and treating it as unavailable. */
            maxBytesPerSource: number;
        };
        /** @description Current state of a submitted research evidence job. Returned both by the submit call's 202 response (initial state, normally "queued") and by GET /ai/research-jobs/{id} (subsequent polls). */
        ResearchJobStatus: {
            /**
             * Format: uuid
             * @description Echoes the jobId from the originating ResearchJobRequest.
             */
            jobId: string;
            /**
             * @description Current lifecycle status of the job.
             * @enum {string}
             */
            status: "queued" | "running" | "succeeded" | "failed" | "cancelled";
            /** @description Per-source retrieval evidence, one entry per requested source, populated as sources are fetched (may be partial while status is "running"). This array carries only what the gateway actually retrieved -- structurally separate from any model-generated narrative text, which this schema has no field for, so a citation a model invents can never be serialized into this array. */
            evidence?: components["schemas"]["EvidenceItem"][];
            error?: components["schemas"]["ResearchJobError"];
            /**
             * Format: date-time
             * @description When the job was accepted onto the queue.
             */
            submittedAt: string;
            /**
             * Format: date-time
             * @description When the job left the queue and began retrieving sources.
             */
            startedAt?: string;
            /**
             * Format: date-time
             * @description When the job reached a terminal status (succeeded/failed/cancelled).
             */
            finishedAt?: string;
        };
        /** @description The retrieval outcome for a single requested source. Present once the gateway has attempted that source, regardless of whether the fetch succeeded. */
        EvidenceItem: {
            /**
             * Format: uri
             * @description Echoes the url from the matching SourceRequest.
             */
            url: string;
            /**
             * @description "retrieved" -- fetched within bounds, fetchedAt/excerpt present. "unavailable" -- the gateway could not fetch it (timeout, network error, exceeded maxDurationMs/maxBytesPerSource). "rejected" -- the gateway declined to fetch it (e.g. disallowed by robots.txt, blocked scheme/host, non-retrievable content-type).
             * @enum {string}
             */
            status: "retrieved" | "unavailable" | "rejected";
            /**
             * Format: date-time
             * @description When this source was fetched. Present only when status is "retrieved".
             */
            fetchedAt?: string;
            /** @description Raw excerpt of the retrieved content -- verbatim from the source, never model-generated. Present only when status is "retrieved". */
            excerpt?: string;
            /** @description The maxDurationMs bound actually enforced for this source, echoed from the matching SourceRequest so the caller can see what was enforced rather than assuming its request was honored verbatim. */
            enforcedMaxDurationMs?: number;
            /** @description The maxBytesPerSource bound actually enforced for this source, echoed from the matching SourceRequest. */
            enforcedMaxBytes?: number;
            /** @description Human-readable detail for why status is "unavailable" or "rejected". Absent when status is "retrieved". */
            reason?: string;
        };
        /** @description Present when status is "failed"; absent otherwise. */
        ResearchJobError: {
            /** @description Machine-readable failure code (e.g. "backend-unavailable", "queue-timeout"). */
            code: string;
            /** @description Human-readable failure detail. */
            message: string;
        };
    };
    responses: never;
    parameters: never;
    requestBodies: never;
    headers: never;
    pathItems: never;
}
export type $defs = Record<string, never>;
export interface operations {
    submitResearchJob: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ResearchJobRequest"];
            };
        };
        responses: {
            /** @description Job accepted onto the queue */
            202: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ResearchJobStatus"];
                };
            };
            /** @description Cost/resource ceiling reached -- request blocked by Treasury (D023) */
            402: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Retrieval backend unavailable */
            503: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    getResearchJobStatus: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Current job status */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ResearchJobStatus"];
                };
            };
            /** @description No research job with this id */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
}
