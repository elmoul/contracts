/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */
export interface paths {
    "/stage/companion/message": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Send one message to the Companion; receive its rung-0 reply
         * @description Every tenant backend implements this route itself (never a direct browser→ai-gateway call). The server is the trust boundary for the pointing contract: `pointAt` in the response is valid only if it appears in this same request's own `pointableTargets` list — the server discards (renders as absent) anything the model returns that isn't in that exact list. The AI selects from an enumerated set; it never generates UI (doctrine R2).
         */
        post: operations["sendCompanionTurn"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
}
export type webhooks = Record<string, never>;
export interface components {
    schemas: {
        /** @description One turn of a Companion conversation — a bounded, client-supplied snapshot of what's honestly on screen, plus the message itself. Everything here grounds the reply; the Companion is instructed to never invent a plant, a status, an event, or a number not stated in this request. */
        CompanionMessageRequest: {
            /**
             * @description The user's message to the Companion.
             * @example Is the garden okay right now?
             */
            message: string;
            /**
             * @description The enumerated, closed set of things the Companion is allowed to point at right now — exactly what is really rendered on screen at the moment of asking. The AI picks a value from this list or returns none; it never invents a target. This is the closed set `pointAt` on the response is validated against — server-side, not merely requested of the model. Absent or empty means nothing is pointable this turn. The reference implementation bounds this list server-side (deduplicated, capped, each entry length-capped) rather than rejecting an oversized list with a 400 — a documented server behavior, not a schema-level constraint.
             * @example [
             *       "card-action-app-health",
             *       "card-data-dimension-event",
             *       "card-data-state-event"
             *     ]
             */
            pointableTargets?: string[];
            stageContext?: components["schemas"]["StageContext"];
            /** @description Prior "I disagree" corrections the user has made this session (client-persisted), most recent last — included so the Companion doesn't repeat a claim the user already corrected (doctrine R8: a correction is reflected in subsequent material). Bounded server-side regardless of what's sent. */
            priorCorrections?: string[];
            /**
             * @description BCP-47-ish locale code the reply should be written in. The reference implementation recognizes "en"/"fr"/"ar"; a blank or unrecognized value falls back to English server-side rather than rejecting the request.
             * @example en
             * @example fr
             * @example ar
             */
            locale?: string;
        };
        /** @description The honest, small snapshot of what the frontend can currently see on `/stage` — built entirely from data already held client-side, never fabricated or looked up server-side. Every field is optional: a card the frontend hasn't resolved yet (still loading) or cannot reach (unavailable) simply omits its own contribution rather than the whole request failing. */
        StageContext: {
            /** @description The app-health card's status when known (e.g. "UP", "DOWN"), else absent. */
            healthStatus?: string;
            /** @description Why the health card itself is unavailable, present only when healthStatus is absent. */
            healthReason?: string;
            /** @description A short plain-text summary of the dimension.event feed already rendered client-side, e.g. "3 recent plant_count events, net +2" or "no recent plant_count activity". */
            dimensionSummary?: string;
            /** @description A short plain-text summary of the state.event feed, mirroring dimensionSummary. */
            stateSummary?: string;
            /**
             * @description True only when the frontend has confirmed a genuine day-zero read (the dimension feed resolved to its designed empty state, not unavailable). Never true by default/absence.
             * @default false
             */
            coldStart: boolean;
        };
        /** @description The Companion's reply — the small, deterministic `{say, pointAt, evidence, confidence}` contract (doctrine R2/R8). `pointAt` is guaranteed by the server to be either absent or one of the exact strings the request's own `pointableTargets` carried — never a value the model invented. `evidence`/`confidence` are a documented pair, not a schema-enforced one (same precedent as `ai.response`'s `skipped` shape, v0.13.0): the reference implementation sends both or neither — present exactly when the reply carries an assessment about the garden/stage to substantiate (doctrine R8's trust slots), absent for a plain conversational reply with nothing to substantiate. */
        CompanionMessageResponse: {
            /** @description The Companion's reply text. */
            say: string;
            /** @description One id from the request's own pointableTargets, or absent if none genuinely fits. Server-validated: never a value outside that list. */
            pointAt?: string;
            /** @description The specific fact backing an assessment just made. Present only together with confidence — see this schema's own description. */
            evidence?: string;
            /**
             * @description Present only together with evidence — see this schema's own description.
             * @enum {string}
             */
            confidence?: "high" | "medium" | "low";
        };
    };
    responses: never;
    parameters: never;
    requestBodies: never;
    headers: never;
    pathItems: never;
}
export type $defs = Record<string, never>;
export interface operations {
    sendCompanionTurn: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CompanionMessageRequest"];
            };
        };
        responses: {
            /** @description Companion replied */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CompanionMessageResponse"];
                };
            };
            /** @description Validation error (e.g. message over 2000 characters) */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Companion rate limit reached */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description ai-gateway disabled or unreachable — the Companion says so honestly, never a fabricated reply */
            503: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
}
