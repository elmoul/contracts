
package io.platform.contracts.cirunner;

import javax.annotation.processing.Generated;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonPropertyDescription;
import com.fasterxml.jackson.annotation.JsonPropertyOrder;


/**
 * BuildCommand
 * <p>
 * Command sent by control-plane to ci-runner requesting that a repo be built at a specific git ref. ci-runner responds with HTTP 202 (acknowledged) and later reports the outcome asynchronously via BuildResult.
 * 
 * 
 */
@JsonInclude(JsonInclude.Include.NON_NULL)
@JsonPropertyOrder({
    "correlationId",
    "repo",
    "ref",
    "workflow"
})
@Generated("jsonschema2pojo")
public class BuildCommand {

    /**
     * Opaque token generated by the caller (control-plane). Echoed in the matching BuildResult so the caller can correlate the async response.
     * 
     * (Required)
     * 
     */
    @JsonProperty("correlationId")
    @JsonPropertyDescription("Opaque token generated by the caller (control-plane). Echoed in the matching BuildResult so the caller can correlate the async response.\n")
    private String correlationId;
    /**
     * Full GitHub repository name — owner/repo, e.g. elmoul/plant-pal
     * (Required)
     * 
     */
    @JsonProperty("repo")
    @JsonPropertyDescription("Full GitHub repository name \u2014 owner/repo, e.g. elmoul/plant-pal")
    private String repo;
    /**
     * Git ref to build: branch name, tag, or full SHA
     * (Required)
     * 
     */
    @JsonProperty("ref")
    @JsonPropertyDescription("Git ref to build: branch name, tag, or full SHA")
    private String ref;
    /**
     * Workflow file name (e.g. ci.yml) or numeric workflow ID. Defaults to the repo's primary workflow when omitted.
     * 
     * 
     */
    @JsonProperty("workflow")
    @JsonPropertyDescription("Workflow file name (e.g. ci.yml) or numeric workflow ID. Defaults to the repo's primary workflow when omitted.\n")
    private String workflow;

    /**
     * No args constructor for use in serialization
     * 
     */
    public BuildCommand() {
    }

    /**
     * 
     * @param ref
     *     Git ref to build: branch name, tag, or full SHA.
     * @param workflow
     *     Workflow file name (e.g. ci.yml) or numeric workflow ID. Defaults to the repo's primary workflow when omitted.
     *     .
     * @param repo
     *     Full GitHub repository name — owner/repo, e.g. elmoul/plant-pal.
     * @param correlationId
     *     Opaque token generated by the caller (control-plane). Echoed in the matching BuildResult so the caller can correlate the async response.
     *     .
     */
    public BuildCommand(String correlationId, String repo, String ref, String workflow) {
        super();
        this.correlationId = correlationId;
        this.repo = repo;
        this.ref = ref;
        this.workflow = workflow;
    }

    /**
     * Opaque token generated by the caller (control-plane). Echoed in the matching BuildResult so the caller can correlate the async response.
     * 
     * (Required)
     * 
     */
    @JsonProperty("correlationId")
    public String getCorrelationId() {
        return correlationId;
    }

    /**
     * Opaque token generated by the caller (control-plane). Echoed in the matching BuildResult so the caller can correlate the async response.
     * 
     * (Required)
     * 
     */
    @JsonProperty("correlationId")
    public void setCorrelationId(String correlationId) {
        this.correlationId = correlationId;
    }

    /**
     * Full GitHub repository name — owner/repo, e.g. elmoul/plant-pal
     * (Required)
     * 
     */
    @JsonProperty("repo")
    public String getRepo() {
        return repo;
    }

    /**
     * Full GitHub repository name — owner/repo, e.g. elmoul/plant-pal
     * (Required)
     * 
     */
    @JsonProperty("repo")
    public void setRepo(String repo) {
        this.repo = repo;
    }

    /**
     * Git ref to build: branch name, tag, or full SHA
     * (Required)
     * 
     */
    @JsonProperty("ref")
    public String getRef() {
        return ref;
    }

    /**
     * Git ref to build: branch name, tag, or full SHA
     * (Required)
     * 
     */
    @JsonProperty("ref")
    public void setRef(String ref) {
        this.ref = ref;
    }

    /**
     * Workflow file name (e.g. ci.yml) or numeric workflow ID. Defaults to the repo's primary workflow when omitted.
     * 
     * 
     */
    @JsonProperty("workflow")
    public String getWorkflow() {
        return workflow;
    }

    /**
     * Workflow file name (e.g. ci.yml) or numeric workflow ID. Defaults to the repo's primary workflow when omitted.
     * 
     * 
     */
    @JsonProperty("workflow")
    public void setWorkflow(String workflow) {
        this.workflow = workflow;
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append(BuildCommand.class.getName()).append('@').append(Integer.toHexString(System.identityHashCode(this))).append('[');
        sb.append("correlationId");
        sb.append('=');
        sb.append(((this.correlationId == null)?"<null>":this.correlationId));
        sb.append(',');
        sb.append("repo");
        sb.append('=');
        sb.append(((this.repo == null)?"<null>":this.repo));
        sb.append(',');
        sb.append("ref");
        sb.append('=');
        sb.append(((this.ref == null)?"<null>":this.ref));
        sb.append(',');
        sb.append("workflow");
        sb.append('=');
        sb.append(((this.workflow == null)?"<null>":this.workflow));
        sb.append(',');
        if (sb.charAt((sb.length()- 1)) == ',') {
            sb.setCharAt((sb.length()- 1), ']');
        } else {
            sb.append(']');
        }
        return sb.toString();
    }

    @Override
    public int hashCode() {
        int result = 1;
        result = ((result* 31)+((this.correlationId == null)? 0 :this.correlationId.hashCode()));
        result = ((result* 31)+((this.ref == null)? 0 :this.ref.hashCode()));
        result = ((result* 31)+((this.workflow == null)? 0 :this.workflow.hashCode()));
        result = ((result* 31)+((this.repo == null)? 0 :this.repo.hashCode()));
        return result;
    }

    @Override
    public boolean equals(Object other) {
        if (other == this) {
            return true;
        }
        if ((other instanceof BuildCommand) == false) {
            return false;
        }
        BuildCommand rhs = ((BuildCommand) other);
        return (((((this.correlationId == rhs.correlationId)||((this.correlationId!= null)&&this.correlationId.equals(rhs.correlationId)))&&((this.ref == rhs.ref)||((this.ref!= null)&&this.ref.equals(rhs.ref))))&&((this.workflow == rhs.workflow)||((this.workflow!= null)&&this.workflow.equals(rhs.workflow))))&&((this.repo == rhs.repo)||((this.repo!= null)&&this.repo.equals(rhs.repo))));
    }

}
