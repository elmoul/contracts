# contracts — `.brain` overrides (hand-owned)

<!--
Hand-maintained. FRESH ADOPTION via `brain-toolkit`'s `bin/adopt` on 2026-08-02
(toolkit v0.6.2). The structurer is the only thing that reads this file
programmatically; until a structurer run has happened here, it is simply a
place human calls will go once there is anything to override. Never
hand-edit `.brain/knowledge/*`, `AGENT.md`, or `.brain/graph.md` directly --
a change belongs here instead.
-->

## pins

<!-- Unit ids pinned to bypass archival and always compose alongside
     conventions. Empty -- no knowledge units exist yet (fresh adoption). -->

(none yet)

## ratified conventions

<!-- Unit ids promoted from `status: proposed` to `status: active` by human
     ratification. Empty -- no structurer run yet. -->

(none yet)

## cleared stale flags

<!-- Unit ids whose `stale-flagged` status was cleared by a human after
     confirming the fact is still true even though its provenance anchor
     moved or disappeared. Empty -- no structurer run yet, so nothing has
     ever been flagged. -->

(none yet)
